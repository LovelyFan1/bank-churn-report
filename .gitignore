# Dependencies
node_modules/
__pycache__/
*.pyc

# Build output
dist/
build/

# Saved models
saved_models/
# 换数据源时留下的模型备份（换回去用得上，但不该进仓库）
saved_models.bak-*/
*.joblib

# Database
*.db

# Environment
.env
.env.local

# 认证签名密钥（未配置 AUTH_SECRET_KEY 时自动生成）
# ⚠ 必须忽略：它是签发/校验令牌的密钥，提交进仓库等于公开，
#   攻击者可据此伪造任意用户（含管理员）的令牌。
#   路径可能出现在两处：Docker 卷内 data/.auth_secret（本就在卷里，
#   不进工作区），以及**本地直接跑**时的 backend/.auth_secret。
.auth_secret

# Python virtual environment
.venv/
venv/

# Bootstrap installer
get-pip.py

# IDE
.vscode/
.idea/
*.sw?

# OS
.DS_Store
Thumbs.db

# Logs
*.log
