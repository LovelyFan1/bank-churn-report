fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.35
pydantic-settings==2.5.0
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.2
xgboost==2.1.1
lightgbm==4.5.0
shap==0.45.0
joblib==1.4.2

# Async task queue
celery[redis]==5.4.0
redis==5.2.1

# ── 智能体编排（对话式任务型 Agent）─────────────────────
# 选型依据（实测，非印象）：
#   · 只装 openai SDK 需 +6 个包；LangGraph 全套需 +32 个包，
#     但实测**全部核心依赖不受影响**（fastapi/sqlalchemy/pydantic/
#     numpy/pandas/scikit-learn/lightgbm/xgboost/celery 均未被触碰），
#     `pip check` 无冲突 —— 即纯增量安装，不动训练与推理链路。
#   · 为什么不用 LangChain 的 Chain：Chain 是线性管道，做不了
#     「条件回退」（数字校验不过就退回模板）与「图内中断」（写操作
#     等人确认），而这两条正是本项目防幻觉的核心机制。
#   · 暂不装 vector store / embedding：MVP 语料仅约 300 块，
#     检索层二期再加，见 app/agent/README.md 的路线说明。
langgraph==0.2.60
langchain-openai==0.2.14

# Production database (for 10M+ scale; SQLite still works for dev)
# psycopg2-binary==2.9.9
