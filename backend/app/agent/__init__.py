"""智能体（对话式任务型 Agent）。

模块职责：
    state.py        LangGraph 的数据契约（唯一）
    guard_rules.py  拒答硬拦截（确定性，不依赖 LLM）
    tools.py        工具注册表（对系统全部可做的动作）
    llm.py          模型接入（全系统唯一出口）
    verify.py       数字保全校验（防幻觉的机械保障）
    graph.py        状态机编排（控制流）

设计原则见各文件顶部注释。核心两条：
  1. 数字一律来自确定性计算，LLM 只负责选工具与措辞
  2. 写操作只能"提议"，执行必须由用户显式确认
"""
